import { Outlet, Router } from "react-router-dom";
import App from "../App";
import { AppRoutes } from "./AppRoutes";

export const myComponent = () => {

    return (
        <>      
          <AppRoutes/>
        </>
    );
}
